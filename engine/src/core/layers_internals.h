#ifndef LAYERS_INTERNALS_H    
#define LAYERS_INTERNALS_H

#include "layers.h"

extern bool request_window_close;
extern LayerStack* handle_layer_events(LayerStack* , Event* );

#endif // LAYERS_INTERNALS_H